# Demo

Living check that catalog agents/skills/rules can ship a real slice: a zero-dependency Node task API plus an accessible HTML UI.

```bash
node check.js
node server.js
```

Then open http://localhost:3741. `check.js` must exit 0.

Author: mhmdreza_rafiei
