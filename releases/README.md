# releases

Downloadable copy of **master**. Not an agentry artifact.

| File | Use |
|------|-----|
| `master.zip` | Unzip-and-use. Root = human PDFs. `context/` = files for the AI. |

Rebuild:

```bash
node scripts/workbook/run.mjs
```

Do not download the whole catalog unless you also want agents, skills, and rules.

Author: mhmdreza_rafiei
